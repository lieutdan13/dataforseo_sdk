name: Code Linting
on:
    push:
        branches: [main]
    pull_request:
        branches: [main]
jobs:
    build:
        runs-on: self-hosted
        steps:
            - name: "Checkout Repository"
              uses: actions/checkout@v3
            - name: "Black Format Check"
              uses: psf/black@stable
